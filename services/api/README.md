# Dispatch API

Phase 0 Flask foundation for the Dispatch emergency response platform.

## Commands

```powershell
uv sync --group dev
uv run dispatch-api
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

export const realtimeTopics = {
  reportUpdates: "dispatch:reports",
  departmentResponses: "dispatch:department-responses",
  notifications: "dispatch:notifications",
  feed: "dispatch:feed",
} as const;
